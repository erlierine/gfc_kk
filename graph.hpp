#include <iostream>
#include <string>
#include <vector>
#include <regex>
#include <map>

using namespace std;
void createFlowGraph(vector<string> &lines);
vector<string> createListOfBB(vector<string> &lines);
